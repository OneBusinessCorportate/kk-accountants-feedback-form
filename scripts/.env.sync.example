# Server-only secrets for the OPTIONAL scripts/sync-problems.mjs runner.
# NEVER commit a filled-in copy and NEVER expose these in the frontend.
# (The frontend uses only VITE_SUPABASE_ANON_KEY — see ../.env.example.)
#
# Not needed at all if you rely on the built-in pg_cron job from
# supabase/migrations/0002_problem_ingestion.sql.

SUPABASE_URL=https://fjsogozwseqoxgddjeig.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-server-only
