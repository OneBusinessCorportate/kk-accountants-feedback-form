# Журнал изменений

Запись формата «что / зачем / результат» обязательна для каждого PR, меняющего код: без неё робот `doc-sync` краснит и мердж заблокирован. Форточка для заведомо тривиальной правки — метка PR `skip-changelog`.

## 2026-07-23
- Автоматические шаблонные рассылки (0035). Что: новый раздел «Рассылки» (`/mailings`) — планировщик на 30 дней по каждому клиенту, ручные вложения по месяцам (ведомость ЗП, отчёт налогов), обзор «по дням» для руководителей, журнал отправленных; правка планируемого сообщения только через кнопку с аудит-логом (кто/что/когда), время отправки не редактируется; язык — параметр компании (RU/AM/ENG). Бот `scripts/mailing_bot.mjs` (Bot API, `@onebusiness_agent_bot`) + cron в `render.yaml`. Зачем: шаблонные уведомления должны уходить только автоматически, бухгалтер их не отправляет вручную. Результат: 707 активных компаний засеяны настройками/расписанием, дедуп против `mqa_chat_mailings` (не дублируем и не перетираем ручную отправку). ВАЖНО: отправка на паузе — `ALLOW_SENDING=false` и жёсткий override только в тестовый чат `-5225180694`; реальным клиентам ничего не уходит до отдельного решения владельца. Бэклог: ловить смену названия чата → пересмотр языка (заведена задача).

## 2026-07-22
- Подключён конвейер разработки: журнал + коммит-gate (`doc-sync`), GPT-ревьюер (`ai-review`), правила для агентов (`AGENTS.md`), защита `main`. Зачем: изменения не должны попадать в прод без следа и независимого ревью. Результат: работа только через PR по единому процессу (канон — `OneBusinessCorportate/.github/dev-process.md`).
